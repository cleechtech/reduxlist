Reduxlist
===

### Fire this bad boy up

Clone that shit and change into the directory.

```
$ npm i // like a boss
$ webpack-dev-server
```

```
ERROR in Cannot find module 'babel-runtime/helpers/interop-require'
 @ multi main
webpack: bundle is now VALID.
^C
➜  reduxlist git:(master) ✗ rm -rf node_modules 
➜  reduxlist git:(master) ✗ npm i babel-core babel-loader babel-preset-es2015 babel-preset-react webpack webpack-dev-server --save-dev
^C
➜  reduxlist git:(master) ✗ npm i babel-core babel-loader babel-preset-es2015 babel-preset-react webpack webpack-dev-server --save-dev
^C
➜  reduxlist git:(master) ✗ npm i babel-core babel-loader babel-preset-es2015 babel-preset-react webpack webpack-dev-server --save-dev
```